---
phase: 10-map-sharing
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/frontend/src/views/ItineraryView.vue
autonomous: false
requirements: [MAP-01, MAP-02, MAP-03, MAP-04, SHARE-01, SHARE-02]

must_haves:
  truths:
    - "Map displays correctly at all breakpoints with markers, polylines, and day routes"
    - "Map-timeline bidirectional sync works in both directions without errors"
    - "Share button copies URL and shows toast"
    - "OG meta tags update on itinerary load"
  artifacts:
    - path: "src/frontend/src/views/ItineraryView.vue"
      provides: "Complete map + timeline integration verified visually"
  key_links:
    - from: "Visual verification"
      to: "All Phase 10 features"
      via: "Manual browser testing"
---

<objective>
Visual verification checkpoint — confirm the complete map sharing feature works end-to-end in the browser

Purpose: Automated tests verify code correctness, but map rendering, marker placement, polyline colors, responsive layout, and toast feedback require visual confirmation. This checkpoint verifies the full user experience.

Output: Verified working map sharing feature across desktop and mobile viewports.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-map-sharing/10-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual verification checkpoint for map & sharing features</name>
  <files>src/frontend/src/views/ItineraryView.vue</files>
  <action>Manual visual and interaction verification of all Phase 10 features in the browser</action>
  <verify>Human visual confirmation of: map rendering, markers, polylines, day selector, bidirectional sync, responsive layout, share button, OG tags, no console errors</verify>
  <done>All Phase 10 features verified working correctly by human tester</done>
  <what-built>
    Complete Phase 10 Map & Sharing feature:
    - Amap JS map with tier-colored markers (★/○/◇ with gold/silver/bronze)
    - Dashed walking polylines between consecutive POIs
    - Day route color-coding (blue/green/orange)
    - Day selector pills above map
    - Bidirectional map-timeline sync (click either side highlights the other)
    - Responsive layout: desktop 50/50 split, mobile stacked
    - ShareButton with clipboard copy and toast
    - Dynamic OG meta tags
  </what-built>
  <how-to-verify>
    1. Start the dev server: `cd src/backend && uvicorn main:app --reload` and `cd src/frontend && npm run dev`
    2. Navigate to an itinerary page at `/itinerary/{id}` (use an ID from the database)
    3. **Map rendering:** Confirm the Amap map appears with markers at correct locations
    4. **Markers:** Verify markers show tier symbols (★ for A-tier, ○ for B-tier, ◇ for C-tier) with correct tier colors
    5. **Polylines:** Verify dashed lines appear between consecutive POIs in the same day
    6. **Day colors:** Day 1 polylines blue, Day 2 green, Day 3 orange
    7. **Day selector:** Click "第1天" / "第2天" pills — map should filter to that day's POIs and auto-zoom
    8. **Timeline→Map:** Click a POI in the timeline — map should pan to the corresponding marker and show InfoWindow
    9. **Map→Timeline:** Click a marker on the map — timeline should scroll to and expand the corresponding POI card
    10. **Desktop layout:** At ≥768px width, verify left-right split (map left, timeline right, 50/50)
    11. **Mobile layout:** At <768px width, verify stacked layout (map 40vh top, timeline below)
    12. **Share button:** Click "分享" — URL should copy to clipboard and toast should appear saying "链接已复制，分享给朋友吧！"
    13. **OG tags:** Check page source — `og:title` and `og:description` should reflect the itinerary title/summary
    14. **No console errors:** Open DevTools — no JavaScript errors related to Amap, markers, or components
  </how-to-verify>
  <resume-signal>Type "approved" if all features work, or describe issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visual verification | Human confirmation of UI behavior |

## STRIDE Threat Register

No new threats — this is a verification-only plan.
</threat_model>

<verification>
- [ ] Map renders with markers and polylines
- [ ] Day selector works with auto-zoom
- [ ] Bidirectional sync works both ways
- [ ] Responsive layout correct at both breakpoints
- [ ] ShareButton copies and shows toast
- [ ] OG meta tags present in page source
- [ ] No console errors
</verification>

<success_criteria>
1. Human confirms all visual and interaction behaviors work correctly
2. No blocking issues found
</success_criteria>

<output>
After completion, create `.planning/phases/10-map-sharing/10-03-SUMMARY.md`
</output>