---
phase: 09-ui-redesign-rich-display
plan: 05
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/frontend/src/views/ItineraryView.vue
autonomous: false
requirements: [UI-01]

must_haves:
  truths:
    - "Chat bubbles use warm palette: coral for user, warm-surface for assistant"
    - "Loading skeleton uses sand-to-warm-gray colors"
    - "Error states use coral text with warm amber background"
    - "Empty states show Ocean Teal icon via NConfigProvider inheritance"
    - "No hardcoded hex colors remain in ItineraryView.vue"
  artifacts:
    - path: "src/frontend/src/views/ItineraryView.vue"
      provides: "Fully warm-themed itinerary display view"
      contains: "var(--color-"
      min_lines: 300
  key_links:
    - from: "ItineraryView.vue"
      to: "tailwind.css @theme"
      via: "CSS custom properties var(--color-*)"
      pattern: "var\\(--color-"
---

<objective>
Migrate ItineraryView.vue -- the most complex view at 363 lines -- from hardcoded hex colors to warm palette CSS custom properties. Covers chat bubbles, loading skeleton, error/empty states, and all UI chrome. Includes final visual verification checkpoint for the entire Phase 9 redesign.

Purpose: ItineraryView is the primary user-facing content view. It must feel warm and inviting, not tech-blue. This completes the global color migration (D-15). The checkpoint verifies the full Phase 9 visual result.

Output: ItineraryView.vue with zero hardcoded hex colors, all using var() references. Human-verified complete Phase 9 visual result.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-redesign-rich-display/09-CONTEXT.md
@.planning/phases/09-ui-redesign-rich-display/09-01-SUMMARY.md
</context>

<interfaces>
<!-- CSS custom properties from Plan 01 -->
```css
--color-sand: #F5E6D3;       --color-sand-light: #FAF0E6;    --color-sand-dark: #E8D5C4;
--color-coral: #FF6B6B;      --color-coral-dark: #E55A5A;     --color-coral-light: #FF8A8A;
--color-ocean: #4ECDC4;      --color-ocean-dark: #3DBDB4;     --color-ocean-light: #6FE0D8;
--color-warm-bg: #FFFAF5;    --color-warm-surface: #FFF8F0;   --color-warm-text: #2D2016;
--color-warm-text-muted: #6B5B4E; --color-warm-border: #E8D5C4;
--color-warm-gray: #E8DDD3;  --color-warm-amber: #F59E0B;     --color-warm-amber-light: #FEF3C7;
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Complete ItineraryView.vue color migration</name>
  <files>src/frontend/src/views/ItineraryView.vue</files>
  <action>
Per D-15: All pages get warm palette via CSS custom properties.
Per D-16: Error states coral text. Per D-17: Empty states Ocean Teal icon. Per D-18: Loading/skeleton sand-to-warm-gray.

In ItineraryView.vue (363 lines), replace ALL hardcoded hex colors with CSS var() references in a single comprehensive pass:

**Chat bubbles:**
- User message bubble: replace `#18a058` background with `var(--color-coral)`, text stays white
- Assistant message bubble: replace `#f4f4f5` background with `var(--color-warm-surface)`, text `var(--color-warm-text)`
- Chat bubble border-radius: warm to 16px

**Message input bar (fixed bottom):**
- Background: `var(--color-warm-bg)` with top border `var(--color-warm-border)`
- Send button: replace any green with `var(--color-coral)`, hover `var(--color-coral-dark)`

**Header area:**
- Any `#18a058` or green references to `var(--color-ocean)` or `var(--color-coral)` depending on context
- Title text: `var(--color-warm-text)`

**Typography and borders:**
- All text colors: `var(--color-warm-text)` (primary) or `var(--color-warm-text-muted)` (secondary)
- All borders: `var(--color-warm-border)`

**Loading skeleton:**
- Replace skeleton placeholder colors with `var(--color-sand)` (start) and `var(--color-warm-gray)` (end)
- NSpin inherits coral accent from NConfigProvider (Plan 01)

**Error state:**
- Error text: `var(--color-coral)` (per D-16)
- Error background: inherited from NAlert override in NConfigProvider (warm amber)
- Any `#e53e3e` or red error text to `var(--color-coral)`

**Empty/not-found state:**
- NEmpty inherits Ocean Teal icon from NConfigProvider (Plan 01, per D-17)
- Any custom empty styling: `var(--color-ocean)` for icons, `var(--color-warm-text-muted)` for text

**Generating state wrapper:**
- Container colors: `var(--color-warm-bg)` for StageProgress wrapper area

**Final sweep:** Run `grep -nE "#[0-9a-fA-F]{3,8}" src/views/ItineraryView.vue` and ensure ZERO matches. Every color must be a CSS var() reference. Common hex patterns to check: #18a058, #f4f4f5, #f0f0f0, #fff, #ffffff, #000, #ed8936, #333, #666, #999, #e53e3e, #e65100, #ccc, #fafafa.

Do NOT change structure, layout, or logic.
  </action>
  <verify>
    <automated>cd src/frontend && ! grep -qE "#[0-9a-fA-F]{3,8}" src/views/ItineraryView.vue && echo "ALL CLEAN" || echo "STILL HAS HEX"; grep -c "var(--" src/views/ItineraryView.vue; npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>ItineraryView.vue has zero hardcoded hex colors. Chat bubbles use coral (user) and warm-surface (assistant). Loading uses sand-to-warm-gray, errors use coral, empty uses Ocean Teal (inherited). Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete Phase 9 UI redesign</name>
  <action>
Human verifies the complete Phase 9 visual result across all pages and components.
  </action>
  <what-built>Complete Phase 9 UI redesign: warm palette across entire app, rich POI cards with tier badges and data source attribution, hero with journey cards, travel-themed progress bar, and full color migration for all views and components.</what-built>
  <how-to-verify>
    1. Start the frontend dev server: `cd src/frontend && npm run dev`
    2. Open the app in browser
    3. **Home page**: Verify warm gradient hero (sand to ocean), 4 journey cards in grid with travel icons, warm textarea with coral generate button
    4. **Generate an itinerary**: Verify warm gradient progress bar with travel stage messages (not 5 dots)
    5. **Itinerary view**: Verify chat bubbles (coral for user, warm-surface for assistant), rich POI cards with tier badges (star/circle/diamond), data source tags (curated/amap/AI SVG icons)
    6. **Navigation**: Verify warm header (coral logo), consistent warm palette across all pages
    7. **Settings/Auth**: Verify warm colors inherited from theme (no structural changes, just color)
    8. **Itinerary list**: Verify warm card colors, no old orange (#ed8936)
    9. Check that NO page still shows the old green (#18a058) or orange (#ed8936) hardcoded colors
  </how-to-verify>
  <verify>Human visual inspection per steps above</verify>
  <done>Human approved the complete Phase 9 visual result as warm, cohesive, and meeting all design decisions.</done>
  <resume-signal>Type "approved" if everything looks warm and cohesive, or describe specific issues to fix</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS vars -> view rendering | Style-only changes, no data flow impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | E | ItineraryView color migration | accept | Pure visual change, no security surface |
</threat_model>

<verification>
1. `cd src/frontend && npm run build` -- no compilation errors
2. `grep -E "#[0-9a-fA-F]{3,8}" src/views/ItineraryView.vue` -- returns nothing
3. ItineraryView contains 10+ var(--color- references
4. Human visual verification checkpoint passed
</verification>

<success_criteria>
- ItineraryView.vue has zero hardcoded hex colors
- Chat bubbles: coral (user), warm-surface (assistant)
- Loading: sand-to-warm-gray skeleton
- Error: coral text, warm amber background (inherited)
- Empty: Ocean Teal icon (inherited from NConfigProvider)
- `npm run build` passes
- Human visual verification approved for entire Phase 9
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-redesign-rich-display/09-05-SUMMARY.md`
</output>
