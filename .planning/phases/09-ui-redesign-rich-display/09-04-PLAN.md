---
phase: 09-ui-redesign-rich-display
plan: 04
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/frontend/src/components/AppHeader.vue
  - src/frontend/src/components/FeedbackWidget.vue
  - src/frontend/src/components/DaySection.vue
  - src/frontend/src/components/ItineraryTimeline.vue
  - src/frontend/src/views/ItineraryListView.vue
  - src/frontend/src/components/AuthModal.vue
  - src/frontend/src/views/SettingsView.vue
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "App header logo and nav use warm palette CSS vars"
    - "Day section header uses warm palette CSS vars"
    - "Itinerary timeline preview banner uses warm palette CSS vars"
    - "Itinerary list view uses warm palette CSS vars"
    - "Feedback widget uses warm palette CSS vars"
    - "Auth modal uses warm palette CSS vars for inline styles"
    - "Settings view uses warm palette CSS vars for inline styles"
    - "No hardcoded hex colors remain in any of these 7 files"
  artifacts:
    - path: "src/frontend/src/components/AppHeader.vue"
      provides: "Warm-styled header navigation"
      contains: "var(--color-"
    - path: "src/frontend/src/components/FeedbackWidget.vue"
      provides: "Warm-styled feedback widget"
      contains: "var(--color-"
    - path: "src/frontend/src/components/DaySection.vue"
      provides: "Warm day section header"
      contains: "var(--color-"
    - path: "src/frontend/src/components/ItineraryTimeline.vue"
      provides: "Warm timeline preview banner"
      contains: "var(--color-"
    - path: "src/frontend/src/views/ItineraryListView.vue"
      provides: "Warm itinerary list cards"
      contains: "var(--color-"
    - path: "src/frontend/src/components/AuthModal.vue"
      provides: "Warm-styled auth modal inline styles"
      contains: "var(--color-"
    - path: "src/frontend/src/views/SettingsView.vue"
      provides: "Warm-styled settings view inline styles"
      contains: "var(--color-"
  key_links:
    - from: "All 7 files"
      to: "tailwind.css @theme"
      via: "CSS custom properties var(--color-*)"
      pattern: "var\\(--color-"
---

<objective>
Migrate all app shell, navigation, day section, timeline, list view, and feedback components from hardcoded hex colors to warm palette CSS custom properties.

Purpose: Per D-15, ALL pages get warm palette via CSS custom properties. These are color-only swaps -- no structural changes. Once done, every component reads colors from the design system.

Output: 7 files with zero hardcoded hex colors, all using var() references from the warm palette.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-redesign-rich-display/09-CONTEXT.md
@.planning/phases/09-ui-redesign-rich-display/09-01-SUMMARY.md
</context>

<interfaces>
<!-- CSS custom properties from Plan 01 -- use these for ALL color references -->
```css
--color-sand: #F5E6D3;       --color-sand-light: #FAF0E6;    --color-sand-dark: #E8D5C4;
--color-coral: #FF6B6B;      --color-coral-dark: #E55A5A;     --color-ocean: #4ECDC4;
--color-warm-bg: #FFFAF5;    --color-warm-surface: #FFF8F0;   --color-warm-text: #2D2016;
--color-warm-text-muted: #6B5B4E; --color-warm-border: #E8D5C4;
--color-warm-gray: #E8DDD3;  --color-warm-amber-light: #FEF3C7;
--radius-card: 16px;         --shadow-card: 0 2px 12px rgba(45,32,22,0.08);
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: AppHeader + FeedbackWidget warm color migration</name>
  <files>src/frontend/src/components/AppHeader.vue, src/frontend/src/components/FeedbackWidget.vue</files>
  <action>
Per D-15: All pages get warm palette via CSS custom properties. Color-only, no structural changes.

**AppHeader.vue** (63 lines):
- Replace `#ed8936` (logo color) with `var(--color-coral)`
- Replace `#4a5568` (nav text) with `var(--color-warm-text-muted)`
- Replace `#ed8936` (hover) with `var(--color-coral-dark)`
- Scan for any other hardcoded hex and replace with appropriate var()

**FeedbackWidget.vue** (63 lines):
- This file uses Tailwind utility classes, NOT hex values in inline styles.
- Explicit class mappings:
  - `text-red-500` → `text-[var(--color-coral)]`
  - `text-gray-500` → `text-[var(--color-warm-text-muted)]`
  - `bg-white/90` → keep as-is (warm surface achieved via CSS custom properties)
- Scan for any other hardcoded colors in Tailwind classes or inline styles and replace similarly
- If using Tailwind arbitrary values: `bg-[var(--color-warm-surface)]`, `text-[var(--color-warm-text)]`
- Ensure sticky bottom bar uses warm background

For both files: do NOT change component structure, layout, or logic -- only color values. Use `grep -nE "#[0-9a-fA-F]{3,8}"` to find every hex value and replace each with the matching warm palette var().
  </action>
  <verify>
    <automated>cd src/frontend && for f in src/components/AppHeader.vue src/components/FeedbackWidget.vue; do echo "=== $f ==="; grep -cE "#[0-9a-fA-F]{3,8}" "$f" 2>/dev/null && echo "HAS HEX" || echo "CLEAN"; grep -c "var(--" "$f"; done</automated>
  </verify>
  <done>AppHeader and FeedbackWidget use CSS var() for all colors. Zero hardcoded hex values remain in either file.</done>
</task>

<task type="auto">
  <name>Task 2: DaySection + ItineraryTimeline + ItineraryListView warm color migration</name>
  <files>src/frontend/src/components/DaySection.vue, src/frontend/src/components/ItineraryTimeline.vue, src/frontend/src/views/ItineraryListView.vue</files>
  <action>
Per D-15: All pages get warm palette via CSS custom properties. Color-only, no structural changes.

**DaySection.vue** (64 lines):
- Replace `#18a058` (day header color) with `var(--color-ocean)`
- Replace `#f0f0f0` (border) with `var(--color-warm-border)`
- Scan for any other hardcoded hex and replace with appropriate var()

**ItineraryTimeline.vue** (60 lines):
- Replace `#fff8e1` (preview banner bg) with `var(--color-sand-light)`
- Replace `#e65100` (preview text) with `var(--color-coral-dark)`
- Replace `#ffe0b2` (preview border) with `var(--color-sand-dark)`
- Scan for any other hardcoded hex and replace with appropriate var()

**ItineraryListView.vue** (59 lines):
- Replace `#ed8936` (city color) with `var(--color-coral)`
- Scan for any other hardcoded hex and replace with appropriate var()
- NEmpty and NSpin will inherit warm colors from NConfigProvider (Plan 01)

For all 3 files: do NOT change component structure, layout, or logic -- only color values. Run `grep -nE "#[0-9a-fA-F]{3,8}"` on each file to find every hex value. Replace each with the matching warm palette var(). Verify ZERO hex patterns remain.
  </action>
  <verify>
    <automated>cd src/frontend && for f in src/components/DaySection.vue src/components/ItineraryTimeline.vue src/views/ItineraryListView.vue; do echo "=== $f ==="; grep -cE "#[0-9a-fA-F]{3,8}" "$f" 2>/dev/null && echo "HAS HEX" || echo "CLEAN"; grep -c "var(--" "$f"; done</automated>
  </verify>
  <done>DaySection, ItineraryTimeline, ItineraryListView use CSS var() for all colors. Zero hardcoded hex values remain in any of the 3 files.</done>
</task>

<task type="auto">
  <name>Task 3: AuthModal + SettingsView warm color migration</name>
  <files>src/frontend/src/components/AuthModal.vue, src/frontend/src/views/SettingsView.vue</files>
  <action>
Per D-15: All pages get warm palette via CSS custom properties. Color-only, no structural changes.

These files have hardcoded inline styles that NConfigProvider won't override, so they must be fixed directly.

**AuthModal.vue**:
- Line 68: Replace `style="color: #e53e3e"` with `style="color: var(--color-coral)"`
- Scan for any other hardcoded hex and replace with appropriate var()

**SettingsView.vue**:
- Line 66: Replace `style="color: #666"` with `style="color: var(--color-warm-text-muted)"`
- Scan for any other hardcoded hex and replace with appropriate var()

For both files: do NOT change component structure, layout, or logic -- only color values. Run `grep -nE "#[0-9a-fA-F]{3,8}"` on each file to find every hex value. Replace each with the matching warm palette var(). Verify ZERO hex patterns remain.
  </action>
  <verify>
    <automated>cd src/frontend && for f in src/components/AuthModal.vue src/views/SettingsView.vue; do echo "=== $f ==="; grep -cE "#[0-9a-fA-F]{3,8}" "$f" 2>/dev/null && echo "HAS HEX" || echo "CLEAN"; grep -c "var(--" "$f"; done</automated>
  </verify>
  <done>AuthModal and SettingsView use CSS var() for all inline styles. Zero hardcoded hex values remain in either file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS vars -> component render | Style-only changes, no data flow impact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | E | Color migration | accept | Pure visual change, no security surface |
</threat_model>

<verification>
1. `cd src/frontend && npm run build` -- no compilation errors
2. All 7 files contain zero hardcoded hex patterns
3. All 7 files contain var(--color- references
</verification>

<success_criteria>
- All 7 files have zero hardcoded hex colors
- All 7 files use var() from the warm palette
- No structural changes -- only color value replacements
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-redesign-rich-display/09-04-SUMMARY.md`
</output>
