---
phase: 09-ui-redesign-rich-display
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/frontend/src/views/HomeView.vue
  - src/frontend/src/components/StageProgress.vue
autonomous: true
requirements: [UI-04, UI-05]

must_haves:
  truths:
    - "Hero shows full-width sand-to-ocean gradient with title and subtitle"
    - "3-4 clickable journey cards displayed in responsive grid"
    - "Input textarea has warm border and background, generate button is coral accent"
    - "Progress bar shows warm gradient, uses currentStage prop (not stage), optional city prop for personalized messages"
    - "Stage messages cycle through 5 travel-themed Chinese messages"
  artifacts:
    - path: "src/frontend/src/views/HomeView.vue"
      provides: "Hero section with gradient, journey cards, warm input area"
      min_lines: 120
    - path: "src/frontend/src/components/StageProgress.vue"
      provides: "Warm gradient progress bar with travel stage messages"
      contains: "stageMessages"
  key_links:
    - from: "HomeView.vue journey card click"
      to: "textarea input"
      via: "click handler sets example text into input"
      pattern: "example|fillInput|handleExample"
    - from: "StageProgress.vue"
      to: "currentStage prop"
      via: "computed stage index from currentStage drives progress width and message"
      pattern: "currentStage|progressPercent"
---

<objective>
Transform the home page into an emotional entry point with warm hero gradient, journey card examples that invite interaction, and travel-themed generation progress that feels like a journey.

Purpose: First impression matters. The hero creates emotional pull and journey cards replace plain NTag examples with mini travel invitations. The progress bar makes generation feel like a journey, not a sterile loading bar.

Output: Redesigned HomeView.vue with gradient hero + journey cards + warm input, and rebuilt StageProgress.vue with warm gradient progress bar + travel messages.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-ui-redesign-rich-display/09-CONTEXT.md
@.planning/phases/09-ui-redesign-rich-display/09-01-SUMMARY.md
</context>

<interfaces>
<!-- CSS custom properties from Plan 01 -->
```css
--color-sand: #F5E6D3;       --color-sand-light: #FAF0E6;    --color-coral: #FF6B6B;
--color-coral-dark: #E55A5A; --color-ocean: #4ECDC4;         --color-ocean-light: #6FE0D8;
--color-warm-text: #2D2016;  --color-warm-text-muted: #6B5B4E;
--color-warm-border: #E8D5C4;--color-warm-surface: #FFF8F0;   --color-warm-bg: #FFFAF5;
--radius-card: 16px;         --shadow-card: 0 2px 12px rgba(45,32,22,0.08);
--shadow-card-hover: 0 8px 24px rgba(45,32,22,0.15); --transition-smooth: all 0.2s ease;
```

<!-- Current HomeView examples array (to be converted to journey cards) -->
```typescript
const examples = [
  '周末想去上海逛逛，喜欢文艺咖啡馆和小众书店',
  '成都三日游，想体验地道川菜和茶馆文化',
  '杭州浪漫周末，喜欢西湖和有格调的餐厅',
  '带父母逛北京，要轻松不累的经典路线',
]
```

<!-- Current StageProgress stages -->
```typescript
const stages = ['intent', 'prefilter', 'generation', 'validation', 'complete']
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Redesign HomeView hero + journey cards + warm input</name>
  <files>src/frontend/src/views/HomeView.vue</files>
  <action>
Per D-11: Hero = full-width gradient (sand to ocean), title+subtitle, 3-4 clickable journey cards below. Responsive: 4-col desktop, 2-col tablet, stacked mobile.
Per D-12: Journey cards replace current NTag examples. Cards = mini travel invitations.
Per D-13: Keep textarea, restyle with warm border/background. Generate button = coral accent.

**Hero section** -- Replace the current hero div:
- Full-width gradient: `background: linear-gradient(135deg, var(--color-sand) 0%, var(--color-ocean-light) 100%)`
- Padding: 60px 24px 40px
- Title "拾途" in large font (48px desktop / 32px mobile), color var(--color-warm-text), font-weight 700
- Subtitle "品味行程生成器 -- 像本地朋友一样帮你规划旅行" in 16px, color var(--color-warm-text-muted)

**Journey cards** -- Replace the NTag examples section:
- CSS Grid: `display: grid; grid-template-columns: repeat(4, 1fr)`
- Responsive breakpoints: max-width 1024px -> repeat(2, 1fr); max-width 640px -> 1fr
- Gap: 16px, max-width: 960px, margin: 0 auto
- Each card: border-radius var(--radius-card), box-shadow var(--shadow-card), white bg, padding 20px, cursor: pointer
- On hover: box-shadow var(--shadow-card-hover), translateY(-2px), transition var(--transition-smooth)
- Card content: a small travel SVG icon at top (var(--color-coral), 24x24) + example text (14px, line-height 1.6, var(--color-warm-text))
- On click: fills textarea with the example text (keep existing click behavior)

Journey card SVG icons (one per card, inline, NOT emoji):
1. Compass: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><circle cx="12" cy="12" r="10"/><polygon points="16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76" fill="currentColor"/></svg>`
2. Camera: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M23 19a2 2 0 01-2 2H3a2 2 0 01-2-2V8a2 2 0 012-2h4l2-3h6l2 3h4a2 2 0 012 2z"/><circle cx="12" cy="13" r="4"/></svg>`
3. Coffee: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M18 8h1a4 4 0 010 8h-1M2 8h16v9a4 4 0 01-4 4H6a4 4 0 01-4-4V8zM6 1v3M10 1v3M14 1v3"/></svg>`
4. Mountain: `<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M8 3l4 8 5-6 5 15H2L8 3z"/></svg>`

**Input area** -- Restyle the textarea section:
- Background: var(--color-warm-surface)
- Border: 2px solid var(--color-warm-border), on focus 2px solid var(--color-coral)
- Border-radius: 12px
- Generate button: background var(--color-coral), white text, border-radius 12px, hover var(--color-coral-dark)

Remove ALL hardcoded hex colors (#18a058, etc.). Use var() exclusively in scoped CSS.
Keep StageProgress and ItineraryTimeline component imports and integration unchanged.
  </action>
  <verify>
    <automated>cd src/frontend && grep -c "grid-template\|journey" src/views/HomeView.vue && grep -c "var(--" src/views/HomeView.vue && ! grep -qE "#[0-9a-fA-F]{3,8}" src/views/HomeView.vue && echo "NO HEX" || echo "HAS HEX"</automated>
  </verify>
  <done>HomeView has gradient hero (sand to ocean), 4 journey cards in responsive grid with travel SVG icons, warm-styled textarea with coral generate button, no hardcoded hex colors</done>
</task>

<task type="auto">
  <name>Task 2: Replace StageProgress with warm gradient progress bar + travel messages</name>
  <files>src/frontend/src/components/StageProgress.vue</files>
  <action>
Per D-09: Warm gradient progress bar + travel-themed stage messages. Replace 5-dot StageProgress.
Per D-10: Stage messages include city name when available via the optional `city` prop.

**Complete rewrite of StageProgress.vue** — keep the existing props `currentStage: string` and `message?: string`. Add a new optional prop `city?: string`. The `stages` array is an INTERNAL const, NOT a prop (it already exists as a local constant in the component). Do NOT add `stages` or `stage` as props — the actual prop is `currentStage`.

**Props interface:**
```typescript
const props = defineProps<{
  currentStage: string
  message?: string
  city?: string
}>()
```

**Template:**
1. Progress bar track: full width, height 8px, background var(--color-sand), border-radius 4px
2. Progress bar fill: width computed from `currentStage` index (0=10%, 1=30%, 2=60%, 3=85%, 4=100%), gradient `background: linear-gradient(90deg, var(--color-sand) 0%, var(--color-coral) 50%, var(--color-ocean) 100%)`, border-radius 4px, `transition: width 0.5s ease`
3. Stage message below: centered, 14px, color var(--color-warm-text-muted). Use Vue Transition with fade for message changes.

**Script:**
```typescript
const stages = ['intent', 'prefilter', 'generation', 'validation', 'complete']

const getStageMessage = (stage: string, city?: string): string => {
  const messages: Record<string, string> = {
    intent: `正在理解你的${city ? city + '之旅' : '旅行'}需求...`,
    prefilter: `在${city || ''}寻找有趣的地方...`,
    generation: `为你规划${city ? city + '的' : ''}精彩行程...`,
    validation: '验证路线和时间的合理性...',
    complete: `你的${city ? city + '之旅' : '旅程'}准备好了！`,
  }
  return messages[stage] || messages.intent
}

const currentMessage = computed(() => props.message || getStageMessage(props.currentStage, props.city))

const progressPercent = computed(() => {
  const idx = stages.indexOf(props.currentStage)
  if (idx < 0) return 10
  return [10, 30, 60, 85, 100][idx] || 10
})
```

If `props.message` is provided, use it (backward compatibility). Otherwise, generate the city-aware message via `getStageMessage`.

On complete (stage 4): message color changes to var(--color-ocean), font-weight 600.
Add subtle shimmer/pulse animation on progress bar while in progress (stages 0-3).

Remove the 5 colored dots, stage labels, and ALL hardcoded hex colors. Replace with var() references.
  </action>
  <verify>
    <automated>cd src/frontend && grep -c "stageMessages\|progressPercent\|gradient" src/components/StageProgress.vue && grep -c "var(--" src/components/StageProgress.vue && ! grep -qE "#[0-9a-fA-F]{3,8}" src/components/StageProgress.vue && echo "NO HEX" || echo "HAS HEX"</automated>
  </verify>
  <done>StageProgress shows warm gradient progress bar (sand to coral to ocean), cycles through 5 travel-themed Chinese messages, no 5-dot system, no hardcoded hex colors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input -> journey cards | Example text is hardcoded, not user-generated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | I | Stage progress messages | accept | Messages are static constants |
</threat_model>

<verification>
1. `cd src/frontend && npm run build` -- no compilation errors
2. HomeView hero has gradient background
3. Journey cards render in responsive grid
4. StageProgress shows gradient bar instead of dots
5. No hardcoded hex colors in either file
</verification>

<success_criteria>
- HomeView hero has sand-to-ocean gradient with title and subtitle
- Journey cards display as clickable mini invitations in responsive grid
- Textarea has warm border, coral generate button
- StageProgress replaced with warm gradient progress bar
- All 5 travel-themed stage messages appear correctly
- Zero hardcoded hex colors in both files
- `npm run build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-redesign-rich-display/09-03-SUMMARY.md`
</output>
