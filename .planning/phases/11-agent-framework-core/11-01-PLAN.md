---
phase: 11-agent-framework-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/backend/llm/client.py
  - src/backend/tools/__init__.py
  - src/backend/tools/result.py
  - src/backend/tools/registry.py
  - config.yml
  - src/backend/config.py
autonomous: true
requirements:
  - AGENT-02

must_haves:
  truths:
    - "Tool definitions load from config.yml and expose as OpenAI function-calling format"
    - "ToolRegistry validates tool schemas and maps tool names to Python callables"
    - "LLMClient supports three modes: stream_chat, generate_json, and tool_chat"
    - "All existing callers of DeepSeekClient still compile and work (renamed to LLMClient)"
  artifacts:
    - path: "src/backend/tools/__init__.py"
      provides: "Package init for tools module"
      min_lines: 3
    - path: "src/backend/tools/result.py"
      provides: "ToolResult class with data, error, summary fields"
      contains: "class ToolResult"
    - path: "src/backend/tools/registry.py"
      provides: "ToolRegistry class for loading and validating tool definitions"
      contains: "class ToolRegistry"
    - path: "config.yml"
      provides: "Centralized tool schema definitions"
      min_lines: 10
    - path: "src/backend/llm/client.py"
      provides: "LLMClient with stream_chat, generate_json, tool_chat"
      contains: "class LLMClient"
  key_links:
    - from: "src/backend/tools/registry.py"
      to: "config.yml"
      via: "YAML loading at startup"
      pattern: "yaml\\.safe_load|config\\.yml"
    - from: "src/backend/tools/registry.py"
      to: "src/backend/tools/result.py"
      via: "ToolResult return type"
      pattern: "ToolResult"
    - from: "src/backend/llm/client.py"
      to: "openai"
      via: "AsyncOpenAI for tool calling"
      pattern: "AsyncOpenAI"
---

<objective>
Build the foundational components for the agent tool system: (1) replace DeepSeekClient with a new LLMClient supporting tool-calling, (2) create the ToolRegistry that loads tool schemas from config.yml, and (3) define the ToolResult unified output format.

Purpose: These are the building blocks that AgentLoop (Plan 02) depends on. Without tool calling support in the LLM client and a working tool registry, the agent loop cannot function.

Output: LLMClient with 3 methods, ToolRegistry class, ToolResult class, config.yml with tool definitions.
</objective>

<execution_context>
@/home/ubuntu/coding/travel/.opencode/get-shit-done/workflows/execute-plan.md
@/home/ubuntu/coding/travel/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-framework-core/11-CONTEXT.md
@.planning/research/FEATURES.md
@.planning/research/STACK.md
@.planning/REQUIREMENTS.md
@src/backend/llm/client.py
@src/backend/config.py
@src/backend/api/dependencies.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ToolResult + ToolRegistry + config.yml</name>
  <files>
    src/backend/tools/__init__.py
    src/backend/tools/result.py
    src/backend/tools/registry.py
    config.yml
  </files>
  <read_first>
    src/backend/config.py
    src/backend/api/dependencies.py
    .planning/phases/11-agent-framework-core/11-CONTEXT.md (decisions D-09 through D-14)
  </read_first>
  <action>
Per D-09 through D-14:

1. Create `src/backend/tools/__init__.py` — re-export `ToolResult` and `ToolRegistry`.

2. Create `src/backend/tools/result.py` — define `ToolResult` dataclass (per D-13):
   - `data: Any` — the actual result (any type)
   - `error: str | None` — error message if failed, None if success
   - `summary: str` — human-readable Chinese summary for SSE display
   - Include a `@property success -> bool` that returns `self.error is None`
   - Include a `to_dict() -> dict` method that serializes to a dict suitable for sending to LLM as tool result content

3. Create `config.yml` in project root (per D-09) — initial tool definitions for Phase 11 scaffolding. Include one example tool definition to demonstrate the schema format:
   ```yaml
   tools:
     - name: search_pois
       description: "搜索指定城市的POI（景点、餐厅、咖啡馆等），支持按关键词和分类筛选"
       parameters:
         type: object
         properties:
           city:
             type: string
             description: "城市名称，如'上海'、'杭州'"
           keyword:
             type: string
             description: "搜索关键词，如'咖啡馆'、'书店'"
           category:
             type: string
             description: "POI分类，如'餐饮'、'景点'、'购物'"
         required: [city]
   ```
   Note: Actual tool implementations come in Phase 12. Phase 11 defines the schema and registry.

4. Create `src/backend/tools/registry.py` — `ToolRegistry` class (per D-11, D-12):
   - `__init__(self, config_path: str = "config.yml")` — loads tool definitions from config.yml
   - `_load_definitions(self) -> list[dict]` — reads and parses YAML, returns list of tool definition dicts
   - `_validate_definition(self, defn: dict) -> None` — validates a tool has name, description, parameters (JSON Schema). Raises ValueError if invalid.
   - `get_openai_tools(self) -> list[dict]` — returns tools in OpenAI function-calling format: `[{"type": "function", "function": {"name": ..., "description": ..., "parameters": ...}}]`
   - `get_tool_names(self) -> list[str]` — returns list of registered tool names
   - `has_tool(self, name: str) -> bool` — checks if a tool is registered
   - Tool registration is static at startup (per D-12) — no dynamic registration methods
   - Use `pyyaml` for YAML parsing (check if installed, add to pyproject.toml if not)
   - Store parsed definitions in `self._definitions: list[dict]` and build `self._name_map: dict[str, dict]` for lookup

5. Add `pyyaml` to `pyproject.toml` dependencies list.
  </action>
  <acceptance_criteria>
    - `src/backend/tools/__init__.py` exists and re-exports ToolResult and ToolRegistry
    - `src/backend/tools/result.py` contains `class ToolResult` with `data`, `error`, `summary` fields
    - `src/backend/tools/registry.py` contains `class ToolRegistry` with `get_openai_tools()` method
    - `src/backend/tools/registry.py` contains `_load_definitions()` that reads config.yml
    - `config.yml` exists in project root with `tools:` top-level key
    - `config.yml` contains at least one tool definition with name, description, parameters
    - `pyproject.toml` contains `pyyaml` in dependencies
    - `grep -r "class ToolResult" src/backend/tools/result.py` exits 0
    - `grep -r "get_openai_tools" src/backend/tools/registry.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ubuntu/coding/travel && python -c "from backend.tools.result import ToolResult; from backend.tools.registry import ToolRegistry; r = ToolRegistry('config.yml'); print(f'Tools: {r.get_tool_names()}'); print(f'OpenAI format: {len(r.get_openai_tools())} tools'); print('OK')" && echo "VERIFIED"</automated>
  </verify>
  <done>
    ToolResult dataclass defined with data/error/summary fields. ToolRegistry loads tool definitions from config.yml, validates them, and exposes in OpenAI function-calling format. config.yml contains example tool definition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace DeepSeekClient with LLMClient (D-01, D-02, D-03)</name>
  <files>
    src/backend/llm/client.py
    src/backend/api/dependencies.py
    src/backend/pipeline/coordinator.py
    src/backend/pipeline/stages/stage1_intent.py
    src/backend/pipeline/stages/stage3_generate.py
    src/backend/api/routes/generate.py
    src/backend/api/routes/adjust.py
    src/backend/api/routes/test_runner.py
    src/backend/services/test_runner.py
  </files>
  <read_first>
    src/backend/llm/client.py
    src/backend/api/dependencies.py
    src/backend/pipeline/coordinator.py
    src/backend/pipeline/stages/stage1_intent.py
    src/backend/pipeline/stages/stage3_generate.py
    src/backend/api/routes/generate.py
    src/backend/api/routes/adjust.py
    src/backend/api/routes/test_runner.py
    src/backend/services/test_runner.py
    .planning/phases/11-agent-framework-core/11-CONTEXT.md (decisions D-01, D-02, D-03)
  </read_first>
  <action>
Per D-01, D-02, D-03:

1. In `src/backend/llm/client.py`:
   - Rename `DeepSeekClient` to `LLMClient` (per D-01)
   - Keep `ChatGPTClient` unchanged (it's for blind test Group C, not part of agent system)
   - Add `tool_chat()` method to `LLMClient` (per D-02):
     ```python
     @retry(
         stop=stop_after_attempt(3),
         wait=wait_exponential(min=1, max=10),
         retry=retry_if_exception_type((APIConnectionError, RateLimitError, APITimeoutError)),
     )
     async def tool_chat(
         self,
         messages: list[dict],
         tools: list[dict],
         temperature: float = 0.7,
         max_tokens: int = 4096,
     ) -> dict:
         """Non-streaming tool-calling completion. Returns dict with 'content' and/or 'tool_calls'."""
         response = await self.client.chat.completions.create(
             model=self.model,
             messages=messages,
             tools=tools,
             temperature=temperature,
             max_tokens=max_tokens,
         )
         message = response.choices[0].message
         result = {"content": message.content}
         if message.tool_calls:
             result["tool_calls"] = [
                 {
                     "id": tc.id,
                     "type": "function",
                     "function": {
                         "name": tc.function.name,
                         "arguments": tc.function.arguments,
                     },
                 }
                 for tc in message.tool_calls
             ]
         return result
     ```
   - Keep `stream_chat()` and `generate_json()` methods intact (per D-02)
   - Keep all existing retry decorators and patterns (per D-03, D-18)
   - Add backward-compatible alias: `DeepSeekClient = LLMClient` at module level for any remaining import paths

2. Update ALL import references across the codebase (these are the exact files that import DeepSeekClient):
   - `src/backend/api/dependencies.py`: Change `from backend.llm.client import ChatGPTClient, DeepSeekClient` → `from backend.llm.client import ChatGPTClient, LLMClient` (also keep `DeepSeekClient` import for alias). Update `get_llm_client()` return type and constructor to use `LLMClient`.
   - `src/backend/pipeline/coordinator.py`: Change TYPE_CHECKING import from `DeepSeekClient` → `LLMClient`. Change constructor type hint `llm_client: "DeepSeekClient"` → `llm_client: "LLMClient"`.
   - `src/backend/pipeline/stages/stage1_intent.py`: Change TYPE_CHECKING import and function parameter type from `DeepSeekClient` → `LLMClient`.
   - `src/backend/pipeline/stages/stage3_generate.py`: Change TYPE_CHECKING import and function parameter type from `DeepSeekClient` → `LLMClient`.
   - `src/backend/api/routes/generate.py`: Change import from `DeepSeekClient` → `LLMClient`. Change type hints on route function parameters.
   - `src/backend/api/routes/adjust.py`: Change import from `DeepSeekClient` → `LLMClient`. Change type hints.
   - `src/backend/api/routes/test_runner.py`: Change import from `DeepSeekClient` → `LLMClient`. Change type hints.
   - `src/backend/services/test_runner.py`: Change import and type hints from `DeepSeekClient` → `LLMClient`.

3. Verify `llm/__init__.py` exports are correct (or update if needed).
  </action>
  <acceptance_criteria>
    - `src/backend/llm/client.py` contains `class LLMClient` (not just `DeepSeekClient`)
    - `src/backend/llm/client.py` contains `async def tool_chat(` method
    - `src/backend/llm/client.py` contains `DeepSeekClient = LLMClient` alias line
    - `src/backend/llm/client.py` still has `stream_chat` and `generate_json` methods
    - `grep -r "DeepSeekClient" src/backend/` returns ONLY the alias line and ChatGPTClient reference in comments — NO other usage
    - `src/backend/api/dependencies.py` uses `LLMClient` in `get_llm_client()` return type
    - `src/backend/pipeline/coordinator.py` uses `LLMClient` in TYPE_CHECKING import and constructor
    - All 4 stage files compile without import errors
    - `grep -r "from backend.llm.client import.*LLMClient" src/backend/` returns matches for all updated files
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ubuntu/coding/travel && python -c "from backend.llm.client import LLMClient, DeepSeekClient; assert LLMClient is DeepSeekClient; assert hasattr(LLMClient, 'tool_chat'); assert hasattr(LLMClient, 'stream_chat'); assert hasattr(LLMClient, 'generate_json'); print('LLMClient: OK')" && echo "VERIFIED"</automated>
  </verify>
  <done>
    LLMClient replaces DeepSeekClient with 3 methods: stream_chat, generate_json, tool_chat. All existing call sites updated. DeepSeekClient remains as alias for backward compatibility. tool_chat sends tools + messages and returns content + tool_calls dict.
  </done>
</task>

</tasks>

<verification>
1. `python -c "from backend.tools import ToolResult, ToolRegistry"` — imports work
2. `python -c "from backend.llm.client import LLMClient, DeepSeekClient; assert LLMClient is DeepSeekClient"` — alias works
3. `python -c "from backend.tools.registry import ToolRegistry; r = ToolRegistry('config.yml'); print(len(r.get_openai_tools()), 'tools loaded')"` — registry loads config
4. `grep -r "DeepSeekClient" src/backend/ | grep -v "alias\|ChatGPTClient\|__pycache__" | grep -v "DeepSeekClient = LLMClient"` — no lingering old references (should return empty or only the alias line)
</verification>

<success_criteria>
- LLMClient class exists with stream_chat, generate_json, and tool_chat methods
- ToolResult dataclass with data/error/summary fields
- ToolRegistry loads config.yml and produces OpenAI function-calling format
- All existing pipeline/route files updated to use LLMClient
- No compilation/import errors across backend
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-framework-core/11-01-SUMMARY.md`
</output>
